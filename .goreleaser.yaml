version: 2

builds:
  - main: .
    binary: forge
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - format: tar.gz
    name_template: "forge_{{ .Os }}_{{ .Arch }}"

changelog:
  sort: asc
